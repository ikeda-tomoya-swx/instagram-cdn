# Instagram CDN

Public image hosting for Instagram carousel posts (Claude Code update notifications).

Each version has its own subdirectory:
- `v2.1.126/` — Claude Code v2.1.126 update carousel (10 images + caption)

Images are served via `https://raw.githubusercontent.com/ikeda-tomoya-swx/instagram-cdn/main/<path>`.
